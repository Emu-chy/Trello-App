const Footer = () => {
    return (
        <div className="text-center border-t-2 py-5">
            <p>{new Date().getFullYear()} &copy; All right reserved to</p>
        </div>
    );
};

export default Footer;
